spring:
  application:
    name: nexten-demo
  cloud:
    kubernetes:
      discovery:
        all-namespaces: true
      reload:
        enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
      cors:
        allowed-origins: '*'
